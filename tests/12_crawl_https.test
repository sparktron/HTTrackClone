#!/bin/sh
set -eu

if test "${HTTPS_SUPPORT:-yes}" = no; then
  echo "HTTPS support is disabled"
  exit 77
fi

. "${srcdir:-.}/fixture-test.sh"
fixture_start
fixture_crawl "$FIXTURE_HTTPS_URL/simple/basic.html" --insecure
fixture_assert_file "$FIXTURE_HTTPS_DIR/simple/basic.html"
